# Smartphones Data on Human Activity
# Tidy up the data, take a subset of the variables and create a summary data frame
# that contains the group means of the data grouped by activity and by subject

library(dplyr)
source("get_data.R")
source("merge_dataframes.R")
source("extract_meas.R")